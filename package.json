{
  "name": "ib-science-qbank",
  "version": "1.0.0",
  "description": "IB Physics and Chemistry Question Bank and Practice App",
  "main": "main.js",
  "author": "Om Modi",
  "license": "ISC",
  "scripts": {
    "build:web": "node build_web.js",
    "deploy:worker": "npm run build:web && wrangler deploy",
    "deploy:pages": "npm run build:web && wrangler pages deploy www --project-name studyib --branch main",
    "deploy:all": "npm run build:web && wrangler deploy && wrangler pages deploy www --project-name studyib --branch main"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.1",
    "@capacitor/filesystem": "^8.1.2"
  }
}
